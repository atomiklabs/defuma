{
  "name": "@atomiklabs/auth",
  "version": "1.0.0",
  "author": "Atomik Labs",
  "license": "MIT",
  "scripts": {
    "install": "yarn --cwd server install && yarn --cwd client install",
    "start": "yarn --cwd client start && yarn --cwd server start"
  }
}
